// 1. Write a program to replace the “Hyder” to “Islam” in the word
// “Hyderabad” and display the result in your browser.
function replace() {
    
    var originalWord = "Hyderabad";
    
    var ToReplace = "Hyderabad";
    
    var replacement = "Islamabad";

    var modifiedWord = originalWord.replace(ToReplace, replacement);

    console.log(modifiedWord);
}
